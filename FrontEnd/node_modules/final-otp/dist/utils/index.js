"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertNumberIntoBytes = exports.createArray = exports.isBase32 = exports.addTrailingZerosByModulo = exports.addLeadingZeros = void 0;
function createArray(length, defaultValue = 0) {
    const result = [];
    for (let i = 0; i < length; ++i) {
        result.push(defaultValue);
    }
    return result;
}
exports.createArray = createArray;
function convertNumberIntoBytes(toConvert) {
    const bytes = createArray(8);
    for (let i = bytes.length - 1; i >= 0; --i) {
        bytes[i] = toConvert & 0xff;
        toConvert >>= 8;
    }
    return bytes;
}
exports.convertNumberIntoBytes = convertNumberIntoBytes;
function isBase32(input) {
    const regex = /^[A-Z0-7]+=*$/g;
    return regex.test(input);
}
exports.isBase32 = isBase32;
function addLeadingZeros(numberAsString, expectedLength) {
    let result = numberAsString;
    while (result.length < expectedLength) {
        result = `0${result}`;
    }
    return result;
}
exports.addLeadingZeros = addLeadingZeros;
function addTrailingZerosByModulo(numberAsString, expectedLength) {
    let result = numberAsString;
    while (result.length % expectedLength !== 0) {
        result = `${result}0`;
    }
    return result;
}
exports.addTrailingZerosByModulo = addTrailingZerosByModulo;
//# sourceMappingURL=index.js.map