import { Digits } from './utils';
export declare class Hotp {
    create(secret: string, counter: number, digits?: Digits): string;
    verify(code: string, secret: string, counter: number): boolean;
    protected createHmac(secret: string, counter: number | string): string;
    protected truncate(hmacResult: string, digits?: Digits): string;
}
