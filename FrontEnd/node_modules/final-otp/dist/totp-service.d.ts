import { Digits } from './utils';
export declare class Totp {
    private hotp;
    create(secret: string, t1?: number, t0?: number, digits?: Digits): string;
    verify(code: string, secret: string, t1?: number, t0?: number): boolean;
}
