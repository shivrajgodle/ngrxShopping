import { Digits } from '../utils';
import { Hotp } from '../models/hotp';
interface Args {
    digits?: Digits;
    expiresIn?: number;
}
export declare namespace HotpService {
    function create(secret: string, counter: number, args?: Args): Hotp | undefined;
    function verify(code: string, secret: string, counter: number): boolean;
}
export {};
