import { Digits } from '../utils';
import { Totp } from '../models/totp';
interface Args {
    timeStepSize?: number;
    t1?: number;
    digits?: Digits;
}
export declare namespace TotpService {
    function create(secret: string, args?: Args): Totp;
    function verify(code: string, secret: string, args?: Args): boolean;
}
export {};
