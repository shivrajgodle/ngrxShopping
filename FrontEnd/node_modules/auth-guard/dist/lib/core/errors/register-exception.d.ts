import { AuthenticationType } from '../models/authentication-types';
import { BaseException } from './base-exception';
import { CredentialException } from './credential-exception';
export interface RegisterAdditionalData {
    [key: string]: {
        [key: string]: any;
    } | undefined;
}
export declare class RegisterException extends BaseException implements CredentialException {
    private readonly type;
    private readonly additionalData;
    constructor(type: AuthenticationType, additionalData?: RegisterAdditionalData);
    getType(): AuthenticationType;
    getData(): RegisterAdditionalData;
}
