"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Random = void 0;
var tslib_1 = require("tslib");
var crypto_1 = tslib_1.__importDefault(require("crypto"));
var Random;
(function (Random) {
    function id(length) {
        if (length === void 0) { length = 8; }
        return cryptoKey(length);
    }
    Random.id = id;
    function cryptoKey(length) {
        if (length === void 0) { length = 32; }
        return crypto_1.default
            .randomBytes(Math.ceil(length / 2))
            .toString('hex')
            .slice(0, length);
    }
    Random.cryptoKey = cryptoKey;
    function randomNumber(length) {
        if (length === void 0) { length = 32; }
        var bytes = [];
        for (var i = 0; i < length; ++i) {
            var byte = Math.round(Math.random() * 255);
            bytes.push(byte);
        }
        var result = bytes.map(function (byte) { return byte.toString(10); }).join('');
        return parseInt(result.slice(0, length), 10);
    }
    Random.randomNumber = randomNumber;
})(Random = exports.Random || (exports.Random = {}));
