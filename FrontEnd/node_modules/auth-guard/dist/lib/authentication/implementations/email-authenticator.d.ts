import { BaseAuthenticator } from './base-authenticator';
import { AuthenticatorValidationResult } from '../interfaces/authenticator';
import { IUser } from '../../core/models/user';
import { ILogger } from '../../util';
export interface EmailAuthenticatorOptions {
    domain: string;
}
export declare class EmailAuthenticator extends BaseAuthenticator {
    private readonly sendMailFn;
    private readonly domain;
    constructor(logger: ILogger, options: EmailAuthenticatorOptions);
    prepareAuthenticationType(user: IUser, value?: any): IUser;
    isAuthenticationTypeMissing(user: IUser, value?: string): AuthenticatorValidationResult;
    private prepareEmailAuthentication;
    private sendEmailWithHotp;
}
