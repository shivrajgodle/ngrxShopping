import { Digits } from 'final-otp';
export declare namespace Authentication {
    interface OtpValues {
        type: 'hotp' | 'totp';
        secret: string;
        digits?: Digits;
        issuer: string;
        to: string;
        period?: number;
        initialCounter?: number;
    }
    interface InitialValues {
        emailSecret?: string;
        hotpUri?: string;
        totpUri?: string;
        password?: string;
        biometrics?: string;
    }
    function otpToUri(options: OtpValues): string;
    function uriToOtp(uri: string): OtpValues;
}
