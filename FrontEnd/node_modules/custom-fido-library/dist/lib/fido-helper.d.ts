/// <reference types="node" />
import { AuthenticationData, CredentialLike, WebAuthnData } from './definitions';
export declare namespace FidoHelper {
    function parseAuthData(buffer: Buffer): AuthenticationData;
    function coseToJwk(cose: any): any;
    function randomNumber(length?: number): string;
    function decodeAttestation(credential: CredentialLike): WebAuthnData;
    function decode(jsonToParse: string, authDataToParse: string | Buffer): WebAuthnData;
    function fromBufferToArrayBuffer(buffer: Buffer | undefined): ArrayBuffer;
    function createChallenge(): string;
    function trimBase64(input: string): string;
}
